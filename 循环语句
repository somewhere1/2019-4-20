#include<stdio.h>
main()
{
   int n,i;
   int score;
   int sum;
   printf("Please input n:");
   scanf("%d",&n);
   for(i=0;i<=n,i++)
   {
      printf("Please input %d ",i);
      sanf("%d",score);
      sum+=score;
   }
   printf("average=%g\n",sum*1/n)
}
#include<stdio.h>
main()
{
  int i;
  int n;
  int temp;
  printf("请输入第%d的成绩"，i);
  scanf("%d",temp);
  while(temp>0)
  {
     n++;
     sum+=score;
     printf("请输入%d的成绩"，n+1)；
     scanf("%d",&temp);
  }
  printf("average=%d",sum*1.0/n);
  return 0;
  
}
